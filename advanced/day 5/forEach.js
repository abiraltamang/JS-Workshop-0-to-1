const fruits =["apple","Mango"];

// fruits.forEach((value, index)=> console.log(value, index)) ;



    
var doForEach = function (fruit, index, array) {
    console.log(fruit, index, array)
}
fruits.forEach(doForEach) ;fruits.forEach() ;